{
  "playwright": {
    "command": "npx",
    "args": ["@playwright/mcp@latest", "--caps=storage,testing,devtools", "--timeout-action=3500", "--timeout-navigation=45000"]
  },
  "google-workspace": {
    "command": "uvx",
    "args": ["workspace-mcp", "--tool-tier", "core"]
  }
}
